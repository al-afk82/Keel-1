[project]
name = "hackathon-drift-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "band-sdk[langgraph]>=1.0.0",
    "langchain-anthropic>=1.4.6",
    "python-dotenv>=1.2.2",
]

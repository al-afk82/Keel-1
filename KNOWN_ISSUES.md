# Known Issues

Tracked issues found during the coordinator integration on 2026-06-18. Severity
is judged against the live demo. Status updated as each is solved.

## 1. Status vocabulary mismatch (severity: high — undercounts drift)

Agents do not emit one shared status vocabulary. The alignment checker returns
`aligned` / `misaligned`, others return `clean` / `violation`, others may say
`drifted` or `gap-found`. The harness logger's `derive_turn_status` only counts a
turn as confirmed when it sees `violation`, `drifted`, or `gap-found`. So an
agent can correctly catch drift and the harness still scores the turn `clean`.
This silently undercounts and would make the dashboard look like it missed a
catch in front of judges.

Fix: normalise statuses at the harness logger so every "this is a problem" word
maps to confirmed. Status: SOLVED.

## 2. Spurious empty harness record per turn (severity: medium — audit noise)

The harness logger graph runs `call_model` a second time on its own logged
reply, which has no findings, so it writes a second empty `clean` record after
the real one. Clutters the audit trail and the dashboard.

Fix: only write to the harness when the incoming payload actually carries
findings. Status: SOLVED.

## 3. Inbound mention tag reaches the eleven specialists (severity: low)

Only the logger and harness logger strip the inbound `@[[uuid]]` tag and the
adapter's `[sender]:` label. The eleven specialists still receive it. It does not
break them today because they feed content to the model, but any specialist that
later parses JSON directly will break exactly as the harness logger did.

Fix: apply `shared.mentions.clean_messages` to each specialist's inbound message.
Status: OPEN — batch pass.

## 4. input_id ownership (severity: high — coordinator contract)

The logger generates an `input_id` but the logger is fire and forget, so the
coordinator never receives it back. The coordinator must generate the id itself
and stamp it into every payload, otherwise verdicts cannot be tied to one
exchange and harness aggregation breaks. `run_chain.py` already does this.

Fix: belongs in main.cpp. Status: OPEN — owned by C++ coordinator.

## 5. Same-agent concurrency collision in the bridge (severity: medium)

The bridge keeps one pending slot per route, keyed by route name. Two concurrent
requests to the same agent collide — the second overwrites the first's slot and
one verdict is lost. Fanning across different agents is safe; duplicate
concurrent calls to one agent are not.

Fix: key pending requests per call, not per route. Status: OPEN.

## 6. Band auth crash loop on a bad key (severity: medium)

The agent try/except only wraps the model call, not the Band connection at
startup. An unlinked or wrong key fails authentication before any model runs, and
pm2 restarts it forever. This is why question-generator loops. A bad key is a
crash loop, not a clean skip.

Fix: catch the startup auth failure and exit cleanly, or back off, so pm2 does
not tight loop. Status: OPEN.

## 7. Agents replay stale room backlog on startup (severity: low)

Agents subscribe to every room they belong to, including old test rooms, and
chew through stale backlog on startup, wasting model calls.

Fix: restart agents clean before a demo; longer term, scope agents to the live
session room only. Status: OPEN — operational.

## 8. question-generator key not linked (severity: high — agent down)

Its Band api key is not linked to an agent, 401 at startup. Needs the key
regenerated by whoever holds Band workspace admin. Not a code fix.

Fix: Band admin regenerates the key. Status: BLOCKED — external.

## 9. C++ coordinator not built on the server (severity: high — owner work)

cmake is absent and the build pulls Drogon from source. The binary belongs to
Ayoub and Iterio in their own environment. `run_chain.py` is the exact contract.
main.cpp BAND_HOST is already pointed at 5055.

Fix: build against the contract. Status: OPEN — owned by C++ team.
